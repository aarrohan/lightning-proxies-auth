import type { Metadata } from "next";
import Content from "./Content";

export const metadata: Metadata = {
  title: "Password Reset Success - Lightning Proxies",
};

export default function setNewPassword() {
  return <Content />;
}
